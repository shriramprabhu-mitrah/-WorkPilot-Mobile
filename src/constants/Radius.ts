import { moderateScale } from '../utils/responsive';

export const Radius = {
  xs: moderateScale(4),
  sm: moderateScale(8),
  md: moderateScale(12),
  lg: moderateScale(16),
  xl: moderateScale(24),
  pill: 999,
  circle: 9999,
};